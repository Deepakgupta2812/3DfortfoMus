[build]
  command = "npm install && chmod -R 755 node_modules/.bin && ./node_modules/.bin/vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NODE_ENV = "production"
  NPM_FLAGS = "--include=dev"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
